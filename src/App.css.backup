/* =========================================================
   INTERNFLOW
   Main application styles
========================================================= */

:root {
  --bg: #080a0f;
  --surface: #10131a;
  --surface-2: #141821;
  --surface-3: #191d27;
  --border: rgba(255, 255, 255, 0.07);
  --border-strong: rgba(255, 255, 255, 0.11);

  --text: #f3f5f9;
  --text-soft: #a5adbd;
  --text-muted: #6f7788;

  --blue: #6d7cff;
  --purple: #a875ff;
  --green: #37d39b;
  --orange: #ffad5c;
  --red: #ff6d7a;

  --shadow:
    0 20px 50px rgba(0, 0, 0, 0.25);

  --radius: 18px;
}

/* =========================
   APP SHELL
========================= */

.app-shell {
  min-height: 100vh;
  display: flex;
  background:
    radial-gradient(
      circle at 70% 0%,
      rgba(98, 79, 255, 0.08),
      transparent 25%
    ),
    var(--bg);
}

/* =========================
   SIDEBAR
========================= */

.sidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;

  width: 250px;

  display: flex;
  flex-direction: column;

  padding: 25px 16px 18px;

  background: rgba(10, 12, 17, 0.94);

  border-right: 1px solid var(--border);

  z-index: 20;
}

.brand {
  display: flex;
  align-items: center;
  gap: 11px;

  padding: 0 10px;
  margin-bottom: 40px;
}

.brand-icon {
  width: 39px;
  height: 39px;

  display: grid;
  place-items: center;

  border-radius: 12px;

  background:
    linear-gradient(
      135deg,
      rgba(117, 105, 255, 0.95),
      rgba(153, 94, 255, 0.7)
    );

  color: white;

  box-shadow:
    0 8px 25px rgba(108, 90, 255, 0.25);
}

.brand h1 {
  margin: 0;

  font-size: 17px;
  font-weight: 750;
  letter-spacing: -0.4px;
}

.brand span {
  display: block;

  margin-top: 2px;

  color: var(--text-muted);

  font-size: 10px;
}

.sidebar-section {
  margin-bottom: 30px;
}

.section-label {
  padding: 0 11px;

  margin: 0 0 9px;

  color: #555d6e;

  font-size: 10px;
  font-weight: 700;

  text-transform: uppercase;
  letter-spacing: 1.2px;
}

.sidebar-item {
  width: 100%;

  display: flex;
  align-items: center;
  gap: 12px;

  padding: 11px 12px;
  margin-bottom: 4px;

  border: 0;
  border-radius: 11px;

  background: transparent;

  color: #7f8797;

  text-align: left;

  transition: 0.2s ease;
}

.sidebar-item:hover {
  color: #e5e8ef;
  background: rgba(255, 255, 255, 0.035);
}

.sidebar-item.active {
  color: white;

  background:
    linear-gradient(
      90deg,
      rgba(108, 101, 255, 0.18),
      rgba(108, 101, 255, 0.06)
    );

  box-shadow:
    inset 2px 0 0 var(--blue);
}

.sidebar-item span:not(.sidebar-badge) {
  font-size: 13px;
  font-weight: 550;
}

.sidebar-badge {
  margin-left: auto;

  min-width: 23px;
  height: 21px;

  display: grid;
  place-items: center;

  padding: 0 6px;

  border-radius: 7px;

  color: #bfc5ff;

  background: rgba(108, 124, 255, 0.12);

  font-size: 10px;
  font-weight: 700;
}

.sidebar-bottom {
  margin-top: auto;
}

.upgrade-card {
  display: flex;
  gap: 10px;

  padding: 13px;

  border: 1px solid rgba(137, 112, 255, 0.15);
  border-radius: 14px;

  background:
    linear-gradient(
      145deg,
      rgba(115, 93, 255, 0.09),
      rgba(115, 93, 255, 0.025)
    );

  margin-bottom: 15px;
}

.upgrade-icon {
  width: 31px;
  height: 31px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 9px;

  background: rgba(140, 116, 255, 0.14);

  color: #a995ff;
}

.upgrade-card strong {
  display: block;

  color: #dcd7ff;

  font-size: 11px;
}

.upgrade-card p {
  margin: 4px 0 0;

  color: #71788a;

  font-size: 10px;
  line-height: 1.4;
}

.profile {
  display: flex;
  align-items: center;
  gap: 10px;

  padding: 10px;

  border-top: 1px solid var(--border);

  color: #737b8c;
}

.profile-avatar {
  width: 33px;
  height: 33px;

  display: grid;
  place-items: center;

  border-radius: 10px;

  background:
    linear-gradient(
      135deg,
      #5967ff,
      #9b5cff
    );

  color: white;

  font-size: 12px;
  font-weight: 750;
}

.profile-info {
  flex: 1;
}

.profile-info strong,
.profile-info span {
  display: block;
}

.profile-info strong {
  color: #e4e7ed;
  font-size: 11px;
}

.profile-info span {
  margin-top: 2px;
  color: #6c7484;
  font-size: 10px;
}

/* =========================
   MAIN
========================= */

.main-content {
  width: calc(100% - 250px);
  margin-left: 250px;

  min-height: 100vh;
}

.topbar {
  height: 72px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 36px;

  border-bottom: 1px solid var(--border);

  background: rgba(8, 10, 15, 0.72);

  backdrop-filter: blur(18px);

  position: sticky;
  top: 0;
  z-index: 15;
}

.breadcrumb {
  display: flex;
  align-items: center;
  gap: 9px;

  color: #656d7d;

  font-size: 12px;
}

.breadcrumb strong {
  color: #cbd0da;
  font-weight: 600;
}

.breadcrumb-dot {
  color: #404654;
}

.topbar-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.search-box {
  width: 245px;
  height: 37px;

  display: flex;
  align-items: center;
  gap: 8px;

  padding: 0 10px;

  border: 1px solid var(--border);
  border-radius: 10px;

  background: rgba(255, 255, 255, 0.025);

  color: #626b7c;
}

.search-box input {
  flex: 1;

  min-width: 0;

  border: 0;
  outline: 0;

  background: transparent;

  color: #e6e9ef;

  font-size: 11px;
}

.search-box input::placeholder {
  color: #5f6776;
}

.search-shortcut {
  padding: 3px 5px;

  border: 1px solid var(--border);

  border-radius: 5px;

  color: #606877;

  font-size: 8px;
}

.icon-button {
  position: relative;

  width: 37px;
  height: 37px;

  display: grid;
  place-items: center;

  border: 1px solid var(--border);
  border-radius: 10px;

  background: rgba(255, 255, 255, 0.025);

  color: #8b93a3;
}

.icon-button:hover {
  color: white;
  background: rgba(255, 255, 255, 0.05);
}

.notification-dot {
  position: absolute;

  width: 6px;
  height: 6px;

  top: 7px;
  right: 8px;

  border-radius: 50%;

  background: #ff6375;

  box-shadow: 0 0 0 2px #0d1016;
}

.notification-wrapper {
  position: relative;
}

.notification-panel {
  position: absolute;

  top: 47px;
  right: 0;

  width: 315px;

  padding: 13px;

  border: 1px solid var(--border-strong);
  border-radius: 15px;

  background: #13161e;

  box-shadow: var(--shadow);

  z-index: 100;
}

.notification-header {
  display: flex;
  justify-content: space-between;

  padding: 5px 6px 11px;

  border-bottom: 1px solid var(--border);

  font-size: 11px;
}

.notification-header span {
  color: #777f91;
}

.notification-item {
  display: flex;
  gap: 10px;

  padding: 11px 6px;

  border-bottom: 1px solid rgba(255, 255, 255, 0.035);
}

.notification-item:last-child {
  border-bottom: 0;
}

.notification-icon {
  width: 27px;
  height: 27px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 8px;
}

.notification-icon.warning {
  color: #ffb663;
  background: rgba(255, 172, 91, 0.1);
}

.notification-icon.success {
  color: #4ee1aa;
  background: rgba(55, 211, 155, 0.1);
}

.notification-icon.info {
  color: #8290ff;
  background: rgba(109, 124, 255, 0.1);
}

.notification-item strong,
.notification-item span {
  display: block;
}

.notification-item strong {
  color: #dfe3eb;
  font-size: 10px;
}

.notification-item span {
  margin-top: 3px;
  color: #747c8c;
  font-size: 9px;
}

.add-button {
  height: 37px;

  display: flex;
  align-items: center;
  gap: 7px;

  padding: 0 14px;

  border: 0;
  border-radius: 10px;

  background:
    linear-gradient(
      135deg,
      #6978ff,
      #8e5bff
    );

  color: white;

  font-size: 11px;
  font-weight: 650;

  box-shadow:
    0 8px 20px rgba(101, 91, 255, 0.18);

  transition: 0.2s ease;
}

.add-button:hover {
  transform: translateY(-1px);

  box-shadow:
    0 11px 25px rgba(101, 91, 255, 0.28);
}

/* =========================
   PAGE CONTENT
========================= */

.page-content {
  max-width: 1450px;

  margin: 0 auto;

  padding: 38px 36px 55px;
}

.welcome-row {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;

  margin-bottom: 26px;
}

.eyebrow {
  margin: 0 0 8px;

  color: #656d7c;

  font-size: 10px;
  font-weight: 650;

  text-transform: uppercase;
  letter-spacing: 1px;
}

.welcome-row h2 {
  margin: 0;

  color: #f3f5f9;

  font-size: 25px;
  letter-spacing: -0.8px;
}

.welcome-text {
  margin: 7px 0 0;

  color: #707888;

  font-size: 11px;
}

.secondary-button {
  height: 34px;

  display: flex;
  align-items: center;
  gap: 7px;

  padding: 0 12px;

  border: 1px solid var(--border);
  border-radius: 9px;

  background: rgba(255, 255, 255, 0.025);

  color: #8d95a5;

  font-size: 10px;
}

.secondary-button:hover {
  color: white;
}

/* =========================
   STAT CARDS
========================= */

.stats-grid {
  display: grid;

  grid-template-columns:
    repeat(4, 1fr);

  gap: 13px;

  margin-bottom: 16px;
}

.stat-card {
  position: relative;

  display: flex;
  align-items: flex-start;
  gap: 12px;

  min-height: 112px;

  padding: 17px;

  border: 1px solid var(--border);
  border-radius: var(--radius);

  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.035),
      rgba(255, 255, 255, 0.012)
    );

  transition: 0.2s ease;
}

.stat-card:hover {
  transform: translateY(-2px);

  border-color: var(--border-strong);
}

.stat-icon {
  width: 38px;
  height: 38px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 11px;
}

.stat-icon.blue {
  color: #8290ff;
  background: rgba(109, 124, 255, 0.11);
}

.stat-icon.purple {
  color: #b080ff;
  background: rgba(168, 117, 255, 0.11);
}

.stat-icon.orange {
  color: #ffb66a;
  background: rgba(255, 173, 92, 0.1);
}

.stat-icon.green {
  color: #4bdfaa;
  background: rgba(55, 211, 155, 0.1);
}

.stat-content span,
.stat-content strong,
.stat-content small {
  display: block;
}

.stat-content span {
  color: #777f90;
  font-size: 10px;
}

.stat-content strong {
  margin-top: 4px;

  color: #f3f5f9;

  font-size: 25px;
  line-height: 1;
  letter-spacing: -1px;
}

.stat-content small {
  margin-top: 7px;

  color: #596273;

  font-size: 9px;
}

.stat-arrow {
  position: absolute;

  top: 17px;
  right: 16px;

  color: #414958;
}

/* =========================
   DASHBOARD GRID
========================= */

.dashboard-grid {
  display: grid;

  grid-template-columns:
    minmax(0, 1.75fr)
    minmax(300px, 0.85fr);

  gap: 16px;
}

.panel {
  border: 1px solid var(--border);
  border-radius: var(--radius);

  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.032),
      rgba(255, 255, 255, 0.012)
    );

  overflow: hidden;
}

.panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 19px 19px 14px;
}

.panel-header.compact {
  padding-bottom: 15px;
}

.panel-header h3 {
  margin: 0;

  color: #e8ebf1;

  font-size: 13px;
  letter-spacing: -0.2px;
}

.panel-header p {
  margin: 5px 0 0;

  color: #656e7e;

  font-size: 9px;
}

.text-button {
  display: flex;
  align-items: center;
  gap: 5px;

  border: 0;

  background: transparent;

  color: #8992ff;

  font-size: 10px;
}

.text-button:hover {
  color: #a7adff;
}

.panel-icon {
  color: #596273;
}

/* =========================
   FILTERS
========================= */

.filter-row {
  display: flex;
  gap: 6px;

  padding: 0 19px 12px;

  overflow-x: auto;
}

.filter-button {
  padding: 6px 9px;

  white-space: nowrap;

  border: 1px solid transparent;
  border-radius: 7px;

  background: transparent;

  color: #697283;

  font-size: 9px;
}

.filter-button:hover {
  color: #cbd0da;
}

.filter-button.active {
  color: #dce0ff;

  border-color: rgba(109, 124, 255, 0.16);

  background: rgba(109, 124, 255, 0.1);
}

/* =========================
   APPLICATIONS
========================= */

.applications-list {
  border-top: 1px solid var(--border);
}

.application-row {
  min-height: 77px;

  display: flex;
  align-items: center;
  gap: 13px;

  padding: 11px 19px;

  border-bottom: 1px solid rgba(255, 255, 255, 0.045);

  transition: 0.18s ease;
}

.application-row:last-child {
  border-bottom: 0;
}

.application-row:hover {
  background: rgba(255, 255, 255, 0.018);
}

.company-logo {
  width: 38px;
  height: 38px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 10px;

  font-size: 14px;
  font-weight: 800;
}

.company-logo.orange {
  color: #ffc274;
  background: rgba(255, 167, 76, 0.12);
}

.company-logo.blue {
  color: #9ba7ff;
  background: rgba(91, 112, 255, 0.12);
}

.company-logo.green {
  color: #70e3b4;
  background: rgba(55, 211, 155, 0.11);
}

.company-logo.purple {
  color: #c196ff;
  background: rgba(157, 92, 255, 0.12);
}

.company-logo.pink {
  color: #ff9bb4;
  background: rgba(255, 93, 140, 0.11);
}

.application-main {
  min-width: 0;
  flex: 1;
}

.application-main strong,
.application-main span {
  display: block;
}

.application-main strong {
  color: #e2e6ed;

  font-size: 11px;
  font-weight: 650;
}

.application-main > span {
  margin-top: 3px;

  overflow: hidden;

  color: #707989;

  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.application-location {
  display: flex;
  align-items: center;
  gap: 3px;

  margin-top: 5px;

  color: #555e6e;

  font-size: 8px;
}

.application-deadline {
  width: 75px;
}

.application-deadline span,
.application-deadline strong {
  display: block;
}

.application-deadline span {
  color: #525b6b;
  font-size: 8px;
}

.application-deadline strong {
  margin-top: 4px;

  color: #9da5b5;

  font-size: 9px;
  font-weight: 600;
}

.status-pill {
  min-width: 88px;

  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 5px;

  padding: 6px 8px;

  border-radius: 7px;

  font-size: 8px;
  font-weight: 650;
}

.status-applied {
  color: #8e9cff;
  background: rgba(109, 124, 255, 0.09);
}

.status-assessment {
  color: #bd91ff;
  background: rgba(168, 117, 255, 0.09);
}

.status-interview {
  color: #ffba72;
  background: rgba(255, 173, 92, 0.09);
}

.status-followup {
  color: #ff8994;
  background: rgba(255, 109, 122, 0.09);
}

.status-offer {
  color: #57dfaa;
  background: rgba(55, 211, 155, 0.09);
}

.status-rejected {
  color: #a0a6b2;
  background: rgba(150, 156, 170, 0.08);
}

.more-button {
  width: 28px;
  height: 28px;

  display: grid;
  place-items: center;

  border: 0;

  background: transparent;

  color: #535b6a;
}

.more-button:hover {
  color: #a2a9b8;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;

  padding: 45px 20px;

  color: #5c6574;

  text-align: center;
}

.empty-state h4 {
  margin: 12px 0 4px;

  color: #c7ccd5;

  font-size: 12px;
}

.empty-state p {
  margin: 0;

  font-size: 9px;
}

/* =========================
   RIGHT COLUMN
========================= */

.right-column {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.upcoming-list {
  padding: 0 18px 15px;

  background: #10121a !important;
  border-radius: 0 0 12px 12px;
  overflow: hidden;
}

.upcoming-list,
.upcoming-list * {
  box-sizing: border-box;
}

.upcoming-content {
  background: transparent !important;
}

.upcoming-content strong,
.upcoming-content span {
  background: transparent !important;
}

.upcoming-arrow {
  background: transparent !important;
  color: #596174;
}

.upcoming-item {
  display: flex;
  align-items: center;
  gap: 10px;

  width: 100%;
  min-height: 59px;
  padding: 10px 0;

  border: 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.045);

  background: #10121a !important;
  color: #cdd2db !important;

  box-sizing: border-box;
  appearance: none;
  -webkit-appearance: none;

  text-align: left;
  box-shadow: none;
}

.upcoming-item:hover {
  background: #171a24 !important;
}

.upcoming-item:last-child {
  border-bottom: 0;
}

.upcoming-item:last-child {
  border-bottom: 0;
}

.date-box {
  width: 37px;
  height: 39px;

  flex-shrink: 0;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  border-radius: 9px;
}

.date-box strong {
  font-size: 13px;
  line-height: 1;
}

.date-box span {
  margin-top: 3px;

  font-size: 7px;
  font-weight: 700;
}

.date-box.danger {
  color: #ff8994;
  background: rgba(255, 109, 122, 0.09);
}

.date-box.purple {
  color: #bb91ff;
  background: rgba(168, 117, 255, 0.09);
}

.date-box.blue {
  color: #8998ff;
  background: rgba(109, 124, 255, 0.09);
}

.upcoming-content {
  flex: 1;
  min-width: 0;
}

.upcoming-content strong,
.upcoming-content span {
  display: block;
}

.upcoming-content strong {
  overflow: hidden;

  color: #cdd2db;

  font-size: 9px;

  text-overflow: ellipsis;
  white-space: nowrap;
}

.upcoming-content span {
  margin-top: 4px;

  overflow: hidden;

  color: #60697a;

  font-size: 8px;

  text-overflow: ellipsis;
  white-space: nowrap;
}

.upcoming-arrow {
  color: #474f5d;
}

/* =========================
   PIPELINE
========================= */

.pipeline-panel {
  padding-bottom: 17px;
}

.pipeline-row {
  padding: 5px 19px;
}

.pipeline-top {
  display: flex;
  justify-content: space-between;

  margin-bottom: 6px;

  color: #707989;

  font-size: 9px;
}

.pipeline-top strong {
  color: #b9bfca;
}

.pipeline-bar {
  width: 100%;
  height: 5px;

  overflow: hidden;

  border-radius: 99px;

  background: rgba(255, 255, 255, 0.045);
}

.pipeline-progress {
  height: 100%;

  border-radius: inherit;
}

.pipeline-progress.blue {
  background: #6d7cff;
}

.pipeline-progress.purple {
  background: #a875ff;
}

.pipeline-progress.orange {
  background: #ffad5c;
}

.pipeline-progress.green {
  background: #37d39b;
}

/* =========================
   AI BANNER
========================= */

.ai-banner {
  display: flex;
  align-items: center;
  gap: 15px;

  margin-top: 16px;
  padding: 18px 20px;

  border: 1px solid rgba(131, 105, 255, 0.16);
  border-radius: var(--radius);

  background:
    radial-gradient(
      circle at 0% 50%,
      rgba(118, 94, 255, 0.11),
      transparent 35%
    ),
    rgba(118, 94, 255, 0.025);
}

.ai-banner-icon {
  width: 44px;
  height: 44px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 13px;

  color: #a995ff;

  background: rgba(139, 111, 255, 0.1);
}

.ai-banner-content {
  flex: 1;
}

.ai-label {
  color: #8e80db;

  font-size: 7px;
  font-weight: 800;

  letter-spacing: 1.2px;
}

.ai-banner h3 {
  margin: 4px 0 4px;

  color: #dcd9ef;

  font-size: 12px;
}

.ai-banner p {
  max-width: 720px;

  margin: 0;

  color: #696f80;

  font-size: 9px;
  line-height: 1.5;
}

.ai-button {
  display: flex;
  align-items: center;
  gap: 7px;

  padding: 9px 13px;

  flex-shrink: 0;

  border: 1px solid rgba(148, 122, 255, 0.2);
  border-radius: 9px;

  background: rgba(132, 106, 255, 0.1);

  color: #b4a5ff;

  font-size: 9px;
  font-weight: 650;
}

/* =========================
   MODAL
========================= */

.modal-backdrop {
  position: fixed;

  inset: 0;

  display: grid;
  place-items: center;

  padding: 20px;

  background: rgba(0, 0, 0, 0.68);

  backdrop-filter: blur(8px);

  z-index: 200;
}

.modal {
  width: min(570px, 100%);

  border: 1px solid var(--border-strong);
  border-radius: 20px;

  background:
    linear-gradient(
      145deg,
      #151922,
      #0e1117
    );

  box-shadow:
    0 30px 80px rgba(0, 0, 0, 0.5);

  overflow: hidden;

  animation: modalIn 0.2s ease;
}

@keyframes modalIn {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;

  padding: 22px;

  border-bottom: 1px solid var(--border);
}

.modal-header > div:first-child {
  position: relative;
  padding-left: 48px;
}

.modal-icon {
  position: absolute;

  left: 0;
  top: 0;

  width: 36px;
  height: 36px;

  display: grid;
  place-items: center;

  border-radius: 10px;

  color: #9d91ff;

  background: rgba(120, 100, 255, 0.1);
}

.modal-header h3 {
  margin: 2px 0 5px;

  color: #eef0f5;

  font-size: 15px;
}

.modal-header p {
  margin: 0;

  color: #687181;

  font-size: 9px;
}

.modal-close {
  width: 30px;
  height: 30px;

  display: grid;
  place-items: center;

  border: 0;
  border-radius: 8px;

  background: rgba(255, 255, 255, 0.035);

  color: #737b8b;
}

.modal-close:hover {
  color: white;
}

.modal form {
  padding: 22px;
}

.form-grid {
  display: grid;

  grid-template-columns: 1fr 1fr;

  gap: 16px;
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.form-group.full {
  grid-column: 1 / -1;
}

.form-group label {
  color: #9aa2b1;

  font-size: 9px;
  font-weight: 650;
}

.form-group input,
.form-group select {
  width: 100%;
  height: 39px;

  padding: 0 11px;

  outline: 0;

  border: 1px solid var(--border);
  border-radius: 9px;

  background: rgba(255, 255, 255, 0.025);

  color: #e2e5ec;

  font-size: 10px;

  transition: 0.2s ease;
}

.form-group input::placeholder {
  color: #555e6e;
}

.form-group input:focus,
.form-group select:focus {
  border-color: rgba(109, 124, 255, 0.45);

  box-shadow:
    0 0 0 3px rgba(109, 124, 255, 0.07);
}

.form-group select option {
  background: #151922;
  color: white;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 8px;

  margin-top: 22px;
  padding-top: 18px;

  border-top: 1px solid var(--border);
}

.cancel-button,
.submit-button {
  height: 37px;

  padding: 0 13px;

  border-radius: 9px;

  font-size: 10px;
  font-weight: 650;
}

.cancel-button {
  border: 1px solid var(--border);

  background: transparent;

  color: #818999;
}

.submit-button {
  display: flex;
  align-items: center;
  gap: 6px;

  border: 0;

  background:
    linear-gradient(
      135deg,
      #6877ff,
      #8d5bff
    );

  color: white;
}

/* =========================
   RESPONSIVE
========================= */

@media (max-width: 1150px) {
  .sidebar {
    width: 215px;
  }

  .main-content {
    width: calc(100% - 215px);
    margin-left: 215px;
  }

  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  .right-column {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 850px) {
  .sidebar {
    width: 68px;
    padding: 18px 9px;
  }

  .brand {
    justify-content: center;
    padding: 0;
  }

  .brand > div:last-child,
  .section-label,
  .sidebar-item span,
  .upgrade-card,
  .profile-info,
  .profile > svg {
    display: none;
  }

  .sidebar-item {
    justify-content: center;
    padding: 12px;
  }

  .sidebar-badge {
    position: absolute;
    margin: -25px 0 0 20px;
  }

  .profile {
    justify-content: center;
  }

  .main-content {
    width: calc(100% - 68px);
    margin-left: 68px;
  }

  .topbar {
    padding: 0 18px;
  }

  .search-box {
    width: 180px;
  }

  .page-content {
    padding: 28px 18px 45px;
  }
}

@media (max-width: 650px) {
  .topbar {
    height: auto;
    min-height: 68px;

    gap: 10px;

    padding: 12px 14px;
  }

  .breadcrumb {
    display: none;
  }

  .topbar-actions {
    width: 100%;
  }

  .search-box {
    flex: 1;
    width: auto;
  }

  .add-button {
    font-size: 0;
    width: 38px;
    padding: 0;
    justify-content: center;
  }

  .add-button svg {
    margin: 0;
  }

  .welcome-row {
    align-items: flex-start;
    gap: 15px;
  }

  .welcome-row h2 {
    font-size: 21px;
  }

  .secondary-button {
    display: none;
  }

  .stats-grid {
    grid-template-columns: 1fr 1fr;
  }

  .stat-card {
    min-height: 100px;
    padding: 13px;
  }

  .stat-content strong {
    font-size: 21px;
  }

  .application-row {
    padding: 12px;
  }

  .application-deadline {
    display: none;
  }

  .status-pill {
    min-width: auto;
  }

  .right-column {
    display: flex;
  }

  .ai-banner {
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .ai-banner-content {
    min-width: calc(100% - 60px);
  }

  .ai-button {
    margin-left: 59px;
  }

  .form-grid {
    grid-template-columns: 1fr;
  }

  .form-group.full {
    grid-column: auto;
  }
}

@media (max-width: 450px) {
  .sidebar {
    display: none;
  }

  .main-content {
    width: 100%;
    margin-left: 0;
  }

  .page-content {
    padding: 22px 12px 35px;
  }

  .stats-grid {
    gap: 8px;
  }

  .stat-card {
    gap: 8px;
    padding: 11px;
  }

  .stat-icon {
    width: 32px;
    height: 32px;
  }

  .stat-content span {
    font-size: 8px;
  }

  .stat-content strong {
    font-size: 19px;
  }

  .stat-content small {
    font-size: 7px;
  }

  .application-main strong {
    font-size: 10px;
  }

  .application-main > span {
    max-width: 125px;
  }

  .status-pill {
    font-size: 7px;
    padding: 5px 6px;
  }
}

/* ================================
   AI INTERNSHIP ANALYZER
================================ */

.ai-modal {
  width: min(760px, 94vw);
  max-height: 90vh;
  overflow-y: auto;
}

.ai-modal textarea {
  width: 100%;
  min-height: 190px;
  resize: vertical;
  box-sizing: border-box;
  font-family: inherit;
  line-height: 1.6;
}

.ai-result {
  margin-top: 20px;
  padding: 18px;
  border: 1px solid rgba(139, 92, 246, 0.25);
  border-radius: 16px;
  background: rgba(139, 92, 246, 0.06);
}

.ai-result-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 18px;
}

.ai-result-header h4 {
  margin: 5px 0 0;
  font-size: 18px;
}

.ai-result-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}

.ai-result-item {
  padding: 14px;
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.ai-result-item span,
.ai-result-section > span {
  display: block;
  margin-bottom: 7px;
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  opacity: 0.6;
}

.ai-result-item strong {
  font-size: 14px;
}

.ai-result-section {
  margin-top: 18px;
}

.ai-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.ai-tags span {
  display: inline-flex;
  padding: 7px 11px;
  border-radius: 999px;
  background: rgba(139, 92, 246, 0.14);
  border: 1px solid rgba(139, 92, 246, 0.22);
  font-size: 12px;
  font-weight: 600;
}

.ai-tags small {
  opacity: 0.6;
}

.submit-button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

@media (max-width: 650px) {
  .ai-result-grid {
    grid-template-columns: 1fr;
  }

  .ai-modal {
    width: 94vw;
  }
}

/* =========================================================
   APPLICATIONS PAGE
   ========================================================= */

.applications-page {
  width: 100%;
  animation: pageFadeIn 0.25s ease;
}

@keyframes pageFadeIn {
  from {
    opacity: 0;
    transform: translateY(4px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.page-heading {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 26px;
}

.page-heading h1 {
  margin: 5px 0 6px;
  font-size: 30px;
  letter-spacing: -0.8px;
}

.page-heading p {
  margin: 0;
  color: #8f9bb3;
  font-size: 14px;
}

.page-eyebrow {
  color: #6f7d99;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.5px;
}

.primary-action {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border: 0;
  border-radius: 10px;
  padding: 11px 16px;
  background: #7c5cff;
  color: white;
  font-weight: 700;
  font-size: 13px;
  cursor: pointer;
  transition:
    transform 0.2s ease,
    opacity 0.2s ease;
}

.primary-action:hover {
  transform: translateY(-1px);
  opacity: 0.92;
}

/* Summary cards */

.application-summary-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 12px;
  margin-bottom: 18px;
}

.application-summary-card {
  text-align: left;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 13px;
  padding: 15px;
  background: rgba(255, 255, 255, 0.025);
  color: #aeb8cc;
  cursor: pointer;
  transition: all 0.2s ease;
}

.application-summary-card:hover,
.application-summary-card.selected {
  border-color: rgba(124, 92, 255, 0.55);
  background: rgba(124, 92, 255, 0.09);
}

.summary-card-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 8px;
  font-size: 11px;
  font-weight: 600;
}

.summary-card-count {
  color: #ffffff;
  font-size: 20px;
  font-weight: 800;
}

.summary-card-line {
  margin-top: 13px;
  height: 3px;
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.06);
}

.summary-card-line span {
  display: block;
  width: 35%;
  height: 100%;
  border-radius: inherit;
  background: #7c5cff;
}

/* Toolbar */

.applications-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  margin-bottom: 18px;
}

.application-search {
  display: flex;
  align-items: center;
  gap: 10px;
  width: min(420px, 100%);
  min-height: 42px;
  padding: 0 13px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.025);
  color: #77849c;
}

.application-search input {
  flex: 1;
  border: 0;
  outline: 0;
  background: transparent;
  color: #e8edf7;
  font-size: 13px;
}

.application-search input::placeholder {
  color: #657189;
}

.application-filter-group {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.applications-page .filter-button {
  border: 1px solid rgba(255, 255, 255, 0.07);
  background: rgba(255, 255, 255, 0.025);
  color: #8792a8;
}

.applications-page .filter-button.active {
  border-color: rgba(124, 92, 255, 0.45);
  background: rgba(124, 92, 255, 0.12);
  color: #d8d0ff;
}

/* Applications panel */

.applications-page .applications-panel {
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.025);
}

.applications-panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 22px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.applications-panel-header h2 {
  margin: 0 0 4px;
  color: #edf1f8;
  font-size: 15px;
}

.applications-panel-header span {
  color: #68758d;
  font-size: 12px;
}

.pipeline-label {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  color: #8e9ab1;
  font-size: 11px;
}

/* Table */

.applications-table-header,
.application-table-row {
  display: grid;
  grid-template-columns: minmax(260px, 2fr) 150px 150px 150px 40px;
  align-items: center;
  gap: 18px;
  padding: 15px 22px;
}

.applications-table-header {
  color: #65718a;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.8px;
  text-transform: uppercase;
  background: rgba(255, 255, 255, 0.015);
}

.application-table-row {
  width: 100%;
  border: 0;
  border-top: 1px solid rgba(255, 255, 255, 0.045);
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
  transition: background 0.18s ease;
}

.application-table-row:hover {
  background: rgba(124, 92, 255, 0.055);
}

.application-company-cell {
  display: flex;
  align-items: center;
  gap: 12px;
  min-width: 0;
}

.company-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 40px;
  width: 40px;
  height: 40px;
  border-radius: 11px;
  background: rgba(124, 92, 255, 0.13);
  color: #c7bdff;
  font-weight: 800;
}

.company-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 0;
}

.company-info strong {
  overflow: hidden;
  color: #edf1f8;
  font-size: 13px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.company-info span {
  overflow: hidden;
  color: #8995aa;
  font-size: 12px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.company-info small {
  display: flex;
  align-items: center;
  gap: 4px;
  color: #68758c;
  font-size: 10px;
}

.application-status {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  width: fit-content;
  padding: 6px 9px;
  border-radius: 999px;
  font-size: 10px;
  font-weight: 700;
}

.status-applied {
  background: rgba(96, 165, 250, 0.1);
  color: #93c5fd;
}

.status-assessment {
  background: rgba(167, 139, 250, 0.1);
  color: #c4b5fd;
}

.status-interview {
  background: rgba(251, 146, 60, 0.1);
  color: #fdba74;
}

.status-offer {
  background: rgba(74, 222, 128, 0.1);
  color: #86efac;
}

.status-rejected {
  background: rgba(248, 113, 113, 0.1);
  color: #fca5a5;
}

.application-date {
  display: flex;
  align-items: center;
  gap: 7px;
  color: #929db1;
  font-size: 11px;
}

.application-deadline {
  display: flex;
  flex-direction: column;
  gap: 3px;
  color: #aeb8c9;
  font-size: 11px;
}

.application-deadline small {
  color: #68758c;
  font-size: 10px;
}

.application-deadline.deadline-warning {
  color: #fbbf24;
}

.application-deadline.deadline-danger {
  color: #f87171;
}

.application-row-action {
  display: flex;
  justify-content: flex-end;
  color: #59667d;
}

/* Empty */

.applications-empty {
  padding: 70px 20px;
  text-align: center;
}

.empty-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 54px;
  height: 54px;
  margin: 0 auto 15px;
  border-radius: 15px;
  background: rgba(124, 92, 255, 0.1);
  color: #9b8cff;
}

.applications-empty h3 {
  margin: 0 0 7px;
  color: #e8edf6;
}

.applications-empty p {
  margin: 0 0 20px;
  color: #77849b;
  font-size: 13px;
}

/* Details modal */

.application-details-modal {
  width: min(720px, calc(100vw - 32px));
  max-height: calc(100vh - 40px);
  overflow-y: auto;
}

.modal-eyebrow {
  color: #7c5cff !important;
  font-size: 10px !important;
  font-weight: 800;
  letter-spacing: 1.4px;
}

.application-detail-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
  margin-top: 22px;
}

.detail-item {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 14px;
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 11px;
  background: rgba(255, 255, 255, 0.025);
}

.detail-item span {
  color: #68758c;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.detail-item strong {
  color: #e7ecf5;
  font-size: 13px;
}

.detail-section {
  margin-top: 20px;
}

.detail-section h3 {
  margin: 0 0 10px;
  color: #dce3ef;
  font-size: 12px;
}

.detail-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
}

.detail-tags span {
  padding: 6px 9px;
  border: 1px solid rgba(124, 92, 255, 0.18);
  border-radius: 7px;
  background: rgba(124, 92, 255, 0.08);
  color: #bdb4f5;
  font-size: 11px;
}

.job-description-preview {
  max-height: 180px;
  overflow-y: auto;
  padding: 13px;
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 10px;
  background: rgba(0, 0, 0, 0.12);
  color: #8995aa;
  font-size: 12px;
  line-height: 1.7;
  white-space: pre-wrap;
}

/* Responsive */

@media (max-width: 1100px) {
  .application-summary-grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .applications-table-header,
  .application-table-row {
    grid-template-columns:
      minmax(220px, 2fr)
      130px
      120px
      120px
      30px;
  }
}

@media (max-width: 800px) {
  .page-heading,
  .applications-toolbar {
    align-items: stretch;
    flex-direction: column;
  }

  .application-summary-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .application-filter-group {
    justify-content: flex-start;
  }

  .applications-table {
    overflow-x: auto;
  }

  .applications-table-header,
  .application-table-row {
    min-width: 760px;
  }
}

/* =========================================================
   APPLICATION DETAIL DRAWER
   ========================================================= */

.application-detail-overlay {
  position: fixed;
  inset: 0;
  z-index: 2000;
  display: flex;
  justify-content: flex-end;
  background: rgba(2, 4, 10, 0.68);
  backdrop-filter: blur(5px);
  animation: detailOverlayIn 0.2s ease;
}

@keyframes detailOverlayIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

.application-detail-drawer {
  display: flex;
  flex-direction: column;
  width: min(540px, 100vw);
  height: 100%;
  background: #0b0e15;
  border-left: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: -20px 0 60px rgba(0, 0, 0, 0.4);
  animation: detailDrawerIn 0.25s ease;
}

@keyframes detailDrawerIn {
  from {
    transform: translateX(100%);
  }

  to {
    transform: translateX(0);
  }
}

.detail-drawer-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  padding: 28px 28px 24px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.07);
}

.detail-company-heading {
  display: flex;
  align-items: center;
  gap: 14px;
  min-width: 0;
}

.detail-company-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 52px;
  width: 52px;
  height: 52px;
  border-radius: 14px;
  background: linear-gradient(
    135deg,
    rgba(124, 92, 255, 0.22),
    rgba(124, 92, 255, 0.08)
  );
  border: 1px solid rgba(124, 92, 255, 0.25);
  color: #c8bfff;
  font-size: 20px;
  font-weight: 800;
}

.detail-eyebrow {
  display: block;
  margin-bottom: 5px;
  color: #8067ff;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 1.5px;
}

.detail-company-heading h2 {
  margin: 0;
  color: #f1f4fa;
  font-size: 21px;
  letter-spacing: -0.4px;
}

.detail-company-heading p {
  margin: 4px 0 0;
  color: #8793a8;
  font-size: 12px;
}

.detail-close-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 34px;
  flex: 0 0 34px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.035);
  color: #8d99ae;
  cursor: pointer;
  transition: 0.2s ease;
}

.detail-close-button:hover {
  background: rgba(255, 255, 255, 0.08);
  color: #ffffff;
}

.detail-drawer-body {
  flex: 1;
  overflow-y: auto;
  padding: 22px 28px 30px;
}

.detail-status-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 15px 17px;
  border: 1px solid rgba(124, 92, 255, 0.17);
  border-radius: 12px;
  background: rgba(124, 92, 255, 0.07);
}

.detail-status-card div:first-child {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.detail-status-card span {
  color: #77849b;
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.detail-status-card strong {
  color: #d9d2ff;
  font-size: 13px;
}

.detail-status-dot {
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: #8b73ff;
  box-shadow: 0 0 12px rgba(139, 115, 255, 0.7);
}

.detail-information-section {
  margin-top: 25px;
}

.detail-section-title {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
  color: #8e9bb2;
}

.detail-section-title svg {
  color: #8b73ff;
}

.detail-section-title h3 {
  margin: 0;
  color: #dce3ef;
  font-size: 12px;
  font-weight: 700;
}

.detail-information-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 9px;
}

.detail-information-item {
  min-width: 0;
  padding: 13px;
  border: 1px solid rgba(255, 255, 255, 0.055);
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.025);
}

.detail-information-item span {
  display: block;
  margin-bottom: 6px;
  color: #65718a;
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.detail-information-item strong {
  display: block;
  overflow: hidden;
  color: #dfe5ef;
  font-size: 12px;
  line-height: 1.4;
  text-overflow: ellipsis;
}

.detail-timeline {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.detail-timeline::before {
  position: absolute;
  left: 15px;
  top: 20px;
  bottom: 20px;
  width: 1px;
  background: rgba(124, 92, 255, 0.18);
  content: "";
}

.detail-timeline-item {
  position: relative;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 0;
}

.timeline-icon {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 31px;
  height: 31px;
  flex: 0 0 31px;
  border: 1px solid rgba(124, 92, 255, 0.18);
  border-radius: 50%;
  background: #0b0e15;
  color: #8d78ff;
}

.timeline-icon.deadline {
  color: #fbbf24;
  border-color: rgba(251, 191, 36, 0.2);
}

.detail-timeline-item > div:last-child {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.detail-timeline-item span {
  color: #69768d;
  font-size: 10px;
}

.detail-timeline-item strong {
  color: #dce3ed;
  font-size: 12px;
}

.detail-tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
}

.detail-tag-list span {
  padding: 6px 9px;
  border: 1px solid rgba(124, 92, 255, 0.18);
  border-radius: 7px;
  background: rgba(124, 92, 255, 0.07);
  color: #bdb5ef;
  font-size: 10px;
}

.document-tags span {
  border-color: rgba(96, 165, 250, 0.16);
  background: rgba(96, 165, 250, 0.06);
  color: #9fc5f4;
}

.detail-job-description,
.detail-notes {
  max-height: 190px;
  overflow-y: auto;
  padding: 14px;
  border: 1px solid rgba(255, 255, 255, 0.055);
  border-radius: 10px;
  background: rgba(0, 0, 0, 0.14);
  color: #8d99ae;
  font-size: 11px;
  line-height: 1.7;
  white-space: pre-wrap;
}

.detail-notes {
  max-height: 120px;
}

.detail-drawer-footer {
  display: flex;
  justify-content: flex-end;
  gap: 9px;
  padding: 16px 28px;
  border-top: 1px solid rgba(255, 255, 255, 0.07);
  background: rgba(8, 10, 16, 0.96);
}

.detail-secondary-button,
.detail-primary-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  min-height: 38px;
  padding: 0 14px;
  border-radius: 9px;
  font-size: 11px;
  font-weight: 700;
  cursor: pointer;
}

.detail-secondary-button {
  border: 1px solid rgba(255, 255, 255, 0.08);
  background: rgba(255, 255, 255, 0.035);
  color: #9ca8bc;
}

.detail-primary-button {
  border: 0;
  background: #7c5cff;
  color: #ffffff;
}

.detail-secondary-button:hover {
  background: rgba(255, 255, 255, 0.07);
}

.detail-primary-button:hover {
  opacity: 0.9;
}

@media (max-width: 600px) {
  .application-detail-drawer {
    width: 100%;
  }

  .detail-drawer-header,
  .detail-drawer-body {
    padding-left: 20px;
    padding-right: 20px;
  }

  .detail-information-grid {
    grid-template-columns: 1fr;
  }

  .detail-drawer-footer {
    padding-left: 20px;
    padding-right: 20px;
  }
}

/* =========================================================
   APPLICATION WORKFLOW
   ========================================================= */

.workflow-summary-card {
  margin-top: 22px;
  padding: 16px;
  border: 1px solid rgba(124, 92, 255, 0.2);
  border-radius: 13px;
  background:
    linear-gradient(
      135deg,
      rgba(124, 92, 255, 0.11),
      rgba(124, 92, 255, 0.025)
    );
}

.workflow-summary-top {
  display: flex;
  justify-content: space-between;
  gap: 15px;
}

.workflow-label {
  display: block;
  color: #8067ff;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 1px;
}

.workflow-summary-top h3 {
  margin: 5px 0 0;
  color: #eef1f8;
  font-size: 14px;
}

.workflow-summary-top > strong {
  color: #a99cff;
  font-size: 17px;
}

.workflow-progress-track {
  height: 5px;
  margin-top: 13px;
  overflow: hidden;
  border-radius: 99px;
  background: rgba(255, 255, 255, 0.07);
}

.workflow-progress-fill {
  height: 100%;
  border-radius: inherit;
  background: #8067ff;
  box-shadow: 0 0 12px rgba(128, 103, 255, 0.55);
  transition: width 0.3s ease;
}

.workflow-summary-card p {
  margin: 12px 0 0;
  color: #8995aa;
  font-size: 11px;
  line-height: 1.65;
}

.workflow-next-action {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 14px;
  padding-top: 13px;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  color: #a99cff;
}

.workflow-next-action > div {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.workflow-next-action span {
  color: #65718a;
  font-size: 8px;
  font-weight: 800;
  letter-spacing: 0.8px;
}

.workflow-next-action strong {
  color: #dce3ef;
  font-size: 11px;
}

.workflow-section {
  margin-top: 24px;
}

.workflow-stages {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.workflow-stage {
  position: relative;
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 8px 0;
}

.workflow-stage-marker {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 29px;
  height: 29px;
  flex: 0 0 29px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 50%;
  background: #0b0e15;
  color: #58647a;
}

.workflow-stage.done .workflow-stage-marker {
  border-color: rgba(74, 222, 128, 0.25);
  color: #4ade80;
}

.workflow-stage.current .workflow-stage-marker {
  border-color: rgba(128, 103, 255, 0.45);
  color: #a99cff;
  box-shadow: 0 0 12px rgba(128, 103, 255, 0.18);
}

.workflow-stage-content {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.workflow-stage-content strong {
  color: #dbe2ed;
  font-size: 11px;
}

.workflow-stage-content span {
  color: #68758b;
  font-size: 9px;
}

.workflow-stage.done .workflow-stage-content span {
  color: #5fbf7b;
}

.workflow-stage.current .workflow-stage-content span {
  color: #a99cff;
}

.detail-edit-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.detail-edit-grid label {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.detail-edit-grid label > span {
  color: #77849a;
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.detail-edit-grid input,
.detail-edit-grid select,
.detail-edit-grid textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 9px;
  outline: none;
  background: rgba(255, 255, 255, 0.035);
  color: #dce3ed;
  font: inherit;
  font-size: 11px;
}

.detail-edit-grid input,
.detail-edit-grid select {
  height: 38px;
  padding: 0 10px;
}

.detail-edit-grid textarea {
  padding: 10px;
  resize: vertical;
}

.detail-edit-grid input:focus,
.detail-edit-grid select:focus,
.detail-edit-grid textarea:focus {
  border-color: rgba(128, 103, 255, 0.55);
  box-shadow: 0 0 0 3px rgba(128, 103, 255, 0.08);
}

.detail-edit-notes {
  grid-column: 1 / -1;
}

@media (max-width: 600px) {
  .detail-edit-grid {
    grid-template-columns: 1fr;
  }

  .detail-edit-notes {
    grid-column: auto;
  }
}

/* =========================================================
   FORM DROPDOWN VISIBILITY FIX
   ========================================================= */

.form-group select {
  color: #f8fafc;
  background: #14161d;
}

.form-group select option {
  color: #111827;
  background: #ffffff;
}

/* Edit application drawer dropdown */
.application-detail-drawer select {
  color: #f8fafc;
  background: #14161d;
}

.application-detail-drawer select option {
  color: #111827;
  background: #ffffff;
}

/* Improve dropdown readability when focused */
.form-group select:focus,
.application-detail-drawer select:focus {
  color: #f8fafc;
  background: #14161d;
  outline: none;
}

/* Selected option */
.form-group select option:checked,
.application-detail-drawer select option:checked {
  color: #ffffff;
  background: #6d4aff;
}

/* =========================================================
   INTERFLOW CALENDAR
   ========================================================= */

.calendar-page {
  width: 100%;
}

.calendar-page-header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 24px;
}

.calendar-page-header h2 {
  margin: 5px 0 6px;
  font-size: 32px;
  letter-spacing: -0.8px;
}

.calendar-controls {
  display: flex;
  align-items: center;
  gap: 12px;
}

.calendar-nav-button {
  width: 38px;
  height: 38px;
  border: 1px solid var(--border-color, #252832);
  border-radius: 10px;
  background: #111318;
  color: #ffffff;
  cursor: pointer;
  font-size: 24px;
  line-height: 1;
}

.calendar-nav-button:hover {
  border-color: #7657ff;
  background: #171329;
}

.calendar-month-title {
  min-width: 150px;
  text-align: center;
  font-weight: 700;
  color: #f8fafc;
}

.calendar-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
}

.calendar-card,
.calendar-sidebar-card {
  border: 1px solid #242731;
  border-radius: 16px;
  background: #0d0f14;
  overflow: hidden;
}

.calendar-weekdays {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  border-bottom: 1px solid #242731;
}

.calendar-weekdays div {
  padding: 13px 10px;
  text-align: center;
  color: #7f8798;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
}

.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
}

.calendar-day {
  min-height: 118px;
  padding: 10px;
  border-right: 1px solid #20232b;
  border-bottom: 1px solid #20232b;
  background: #0d0f14;
}

.calendar-day:nth-child(7n) {
  border-right: none;
}

.calendar-day.empty {
  background: #090b0f;
}

.calendar-day-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  margin-bottom: 8px;
  border-radius: 8px;
  color: #a7adbb;
  font-size: 12px;
  font-weight: 700;
}

.calendar-day.today .calendar-day-number {
  background: #7657ff;
  color: #ffffff;
  box-shadow: 0 0 18px rgba(118, 87, 255, 0.3);
}

.calendar-day-events {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.calendar-event {
  width: 100%;
  padding: 5px 7px;
  border: 0;
  border-radius: 6px;
  text-align: left;
  overflow: hidden;
  cursor: pointer;
  font-size: 10px;
  font-weight: 600;
  color: #e5e7eb;
}

.calendar-event span {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.calendar-event.application {
  background: rgba(59, 130, 246, 0.16);
  color: #93c5fd;
}

.calendar-event.deadline {
  background: rgba(245, 158, 11, 0.16);
  color: #fbbf24;
}

.calendar-event.assessment {
  background: rgba(139, 92, 246, 0.16);
  color: #c4b5fd;
}

.calendar-event.interview {
  background: rgba(34, 197, 94, 0.15);
  color: #86efac;
}

.calendar-event.followup {
  background: rgba(236, 72, 153, 0.15);
  color: #f9a8d4;
}

.calendar-more {
  color: #8b93a5;
  font-size: 9px;
  padding-left: 4px;
}

.calendar-sidebar {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.calendar-sidebar-card {
  padding: 18px;
}

.calendar-sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
}

.calendar-sidebar-header h3 {
  margin: 4px 0 0;
  color: #f8fafc;
  font-size: 18px;
}

.calendar-sidebar-header svg {
  color: #8b6cff;
}

.upcoming-event {
  display: flex;
  width: 100%;
  gap: 11px;
  padding: 12px 0;

  border: 0;
  border-bottom: 1px solid #20232b;

  background: #10121a !important;
  color: #f3f4f6 !important;

  text-align: left;
  cursor: pointer;

  appearance: none;
  -webkit-appearance: none;

  box-shadow: none;
  outline: none;
}

.upcoming-event:hover {
  background: #171a24 !important;
}

.upcoming-event:focus,
.upcoming-event:focus-visible {
  background: #171a24 !important;
  outline: none;
  box-shadow: none;
}

.upcoming-event:active {
  background: #1c202b !important;
}

.upcoming-event:hover {
  background: #171a24 !important;
}

.upcoming-event:focus,
.upcoming-event:focus-visible {
  outline: none;
  background: #171a24 !important;
}

.upcoming-event:active {
  background: #1b1e29 !important;
}

.upcoming-event-info {
  background: transparent !important;
}

.upcoming-event-info strong,
.upcoming-event-info span,
.upcoming-event-info small {
  background: transparent !important;
}

.upcoming-event:last-child {
  border-bottom: none;
}

.event-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 30px;
  height: 30px;
  border-radius: 9px;
}

.event-icon.application {
  background: rgba(59, 130, 246, 0.15);
  color: #60a5fa;
}

.event-icon.deadline {
  background: rgba(245, 158, 11, 0.15);
  color: #fbbf24;
}

.event-icon.assessment {
  background: rgba(139, 92, 246, 0.15);
  color: #a78bfa;
}

.event-icon.interview {
  background: rgba(34, 197, 94, 0.15);
  color: #4ade80;
}

.event-icon.followup {
  background: rgba(236, 72, 153, 0.15);
  color: #f472b6;
}

.upcoming-event-info {
  min-width: 0;
}

.upcoming-event-info strong,
.upcoming-event-info span,
.upcoming-event-info small {
  display: block;
}

.upcoming-event-info strong {
  margin-bottom: 3px;
  color: #f3f4f6;
  font-size: 12px;
}

.upcoming-event-info span {
  overflow: hidden;
  color: #8b93a5;
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.upcoming-event-info small {
  margin-top: 4px;
  color: #6f7788;
  font-size: 10px;
}

.calendar-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 35px 10px;
  text-align: center;
  color: #6f7788;
}

.calendar-empty svg {
  margin-bottom: 10px;
  color: #7657ff;
}

.calendar-empty strong {
  margin-bottom: 5px;
  color: #d7dae2;
  font-size: 13px;
}

.calendar-empty span {
  max-width: 220px;
  font-size: 11px;
  line-height: 1.5;
}

.calendar-legend {
  display: flex;
  flex-direction: column;
  gap: 11px;
  margin-top: 15px;
}

.calendar-legend span {
  display: flex;
  align-items: center;
  gap: 9px;
  color: #a7adbb;
  font-size: 11px;
}

.legend-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
}

.legend-dot.application {
  background: #60a5fa;
}

.legend-dot.deadline {
  background: #fbbf24;
}

.legend-dot.assessment {
  background: #a78bfa;
}

.legend-dot.interview {
  background: #4ade80;
}

.legend-dot.followup {
  background: #f472b6;
}

@media (max-width: 1100px) {
  .calendar-layout {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 700px) {
  .calendar-page-header {
    align-items: flex-start;
    flex-direction: column;
  }

  .calendar-day {
    min-height: 90px;
    padding: 6px;
  }

  .calendar-event {
    font-size: 9px;
  }

  .calendar-sidebar {
    grid-template-columns: 1fr;
  }
}

/* =========================================================
   ANALYTICS PAGE
   ========================================================= */

.analytics-page {
  width: 100%;
  padding-bottom: 40px;
}

.analytics-page-header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 28px;
}

.analytics-page-header h2 {
  margin: 6px 0 8px;
  font-size: 32px;
  line-height: 1.1;
  letter-spacing: -0.8px;
}

.analytics-live-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 9px 13px;
  border: 1px solid rgba(129, 90, 255, 0.28);
  border-radius: 999px;
  background: rgba(92, 64, 180, 0.1);
  color: #b9a6ff;
  font-size: 12px;
  font-weight: 700;
}

.analytics-live-dot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: #8b6cff;
  box-shadow: 0 0 12px rgba(139, 108, 255, 0.8);
}

.analytics-summary-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  margin-bottom: 18px;
}

.analytics-summary-card {
  display: flex;
  align-items: center;
  gap: 14px;
  min-height: 104px;
  padding: 18px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 15px;
  background: rgba(12, 14, 21, 0.88);
}

.analytics-summary-icon {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  border-radius: 12px;
}

.analytics-summary-icon.blue {
  color: #75a7ff;
  background: rgba(74, 121, 255, 0.12);
}

.analytics-summary-icon.purple {
  color: #a98bff;
  background: rgba(139, 92, 246, 0.12);
}

.analytics-summary-icon.orange {
  color: #ffb45e;
  background: rgba(245, 158, 11, 0.12);
}

.analytics-summary-icon.green {
  color: #65d49a;
  background: rgba(34, 197, 94, 0.12);
}

.analytics-summary-card span,
.analytics-summary-card small {
  display: block;
}

.analytics-summary-card span {
  color: #858a9b;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.analytics-summary-card strong {
  display: block;
  margin: 4px 0;
  color: #f4f5f8;
  font-size: 25px;
}

.analytics-summary-card small {
  color: #686d7d;
  font-size: 11px;
}

.analytics-main-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(340px, 0.8fr);
  gap: 16px;
}

.analytics-card {
  min-width: 0;
  padding: 20px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 16px;
  background: rgba(12, 14, 21, 0.88);
}

.analytics-card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 22px;
}

.analytics-card-header svg {
  color: #8d70ff;
}

.analytics-card-header h3 {
  margin: 5px 0 0;
  color: #f3f4f8;
  font-size: 17px;
}

.analytics-funnel {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.analytics-funnel-row {
  display: grid;
  grid-template-columns: 105px minmax(0, 1fr) 42px;
  align-items: center;
  gap: 12px;
}

.analytics-funnel-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.analytics-funnel-label span {
  color: #a8adbc;
  font-size: 12px;
}

.analytics-funnel-label strong {
  color: #f4f5f8;
  font-size: 12px;
}

.analytics-funnel-row small {
  color: #727788;
  text-align: right;
  font-size: 11px;
}

.analytics-bar-track {
  height: 7px;
  overflow: hidden;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.055);
}

.analytics-bar {
  height: 100%;
  min-width: 3px;
  border-radius: inherit;
  background: #8064ff;
}

.analytics-bar.applied {
  background: #638cff;
}

.analytics-bar.assessment {
  background: #8b6cff;
}

.analytics-bar.interview {
  background: #b06cff;
}

.analytics-bar.followup {
  background: #52a9ff;
}

.analytics-bar.offer {
  background: #50d68b;
}

.analytics-bar.rejected {
  background: #ef6f7a;
}

.analytics-health {
  display: grid;
  grid-template-columns: 150px 1fr;
  align-items: center;
  gap: 22px;
  margin-bottom: 22px;
}

.analytics-health-ring {
  display: grid;
  place-items: center;
  width: 145px;
  height: 145px;
  border: 10px solid rgba(126, 96, 255, 0.14);
  border-top-color: #8064ff;
  border-right-color: #a27cff;
  border-radius: 50%;
}

.analytics-health-ring > div {
  text-align: center;
}

.analytics-health-ring strong {
  display: block;
  color: #f6f6fa;
  font-size: 27px;
}

.analytics-health-ring span {
  color: #727788;
  font-size: 10px;
  text-transform: uppercase;
}

.analytics-health-items {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}

.analytics-health-items div {
  padding: 11px;
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.025);
}

.analytics-health-items span {
  display: block;
  color: #777c8d;
  font-size: 10px;
}

.analytics-health-items strong {
  display: block;
  margin-top: 4px;
  color: #f0f1f5;
  font-size: 17px;
}

.analytics-rate-list {
  display: flex;
  flex-direction: column;
  gap: 9px;
  padding-top: 16px;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
}

.analytics-rate-list div {
  display: flex;
  justify-content: space-between;
  color: #8b90a0;
  font-size: 12px;
}

.analytics-rate-list strong {
  color: #dfe2ea;
}

.analytics-skills {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.analytics-skill-row > div:first-child {
  display: flex;
  justify-content: space-between;
  margin-bottom: 7px;
}

.analytics-skill-row span {
  color: #a9adbb;
  font-size: 12px;
}

.analytics-skill-row strong {
  color: #e8e9ee;
  font-size: 11px;
}

.analytics-bar.skill {
  background: #8b6cff;
}

.deadline-summary {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-bottom: 16px;
}

.deadline-summary-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 13px;
  border-radius: 11px;
}

.deadline-summary-item.danger {
  color: #ff858e;
  background: rgba(239, 68, 68, 0.08);
}

.deadline-summary-item.warning {
  color: #ffc166;
  background: rgba(245, 158, 11, 0.08);
}

.deadline-summary-item strong,
.deadline-summary-item span {
  display: block;
}

.deadline-summary-item strong {
  color: #f1f2f5;
  font-size: 17px;
}

.deadline-summary-item span {
  color: #777c8c;
  font-size: 10px;
}

.deadline-list {
  display: flex;
  flex-direction: column;
}

.deadline-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  width: 100%;
  padding: 12px 0;
  border: 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
}

.deadline-item:last-child {
  border-bottom: 0;
}

.deadline-item:hover strong {
  color: #a993ff;
}

.deadline-item strong,
.deadline-item span {
  display: block;
}

.deadline-item strong {
  color: #e9eaf0;
  font-size: 12px;
}

.deadline-item span {
  margin-top: 3px;
  color: #717687;
  font-size: 10px;
}

.deadline-item small {
  flex-shrink: 0;
  font-size: 10px;
  font-weight: 700;
}

.danger-text {
  color: #ff858e;
}

.warning-text {
  color: #ffc166;
}

.analytics-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 170px;
  gap: 8px;
  color: #777c8d;
  text-align: center;
}

.analytics-empty svg {
  color: #8064ff;
  margin-bottom: 3px;
}

.analytics-empty strong {
  color: #b9bdc8;
  font-size: 13px;
}

.analytics-empty span {
  max-width: 260px;
  font-size: 11px;
  line-height: 1.5;
}

.analytics-empty.compact {
  min-height: 130px;
}

.analytics-recent-card {
  margin-top: 16px;
}

.analytics-count {
  display: grid;
  place-items: center;
  min-width: 27px;
  height: 27px;
  padding: 0 8px;
  border-radius: 8px;
  color: #bcaeff;
  background: rgba(126, 96, 255, 0.1);
  font-size: 11px;
  font-weight: 700;
}

.analytics-recent-list {
  display: flex;
  flex-direction: column;
}

.analytics-recent-item {
  display: flex;
  align-items: center;
  gap: 13px;
  width: 100%;
  padding: 13px 0;
  border: 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
}

.analytics-recent-item:last-child {
  border-bottom: 0;
}

.analytics-recent-item:hover
  .analytics-recent-info strong {
  color: #a993ff;
}

.analytics-company-avatar {
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  flex-shrink: 0;
  border-radius: 9px;
  color: #c4b8ff;
  background: rgba(111, 84, 214, 0.15);
  font-size: 12px;
  font-weight: 800;
}

.analytics-recent-info {
  min-width: 0;
  flex: 1;
}

.analytics-recent-info strong,
.analytics-recent-info span {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.analytics-recent-info strong {
  color: #e8e9ee;
  font-size: 12px;
}

.analytics-recent-info span {
  margin-top: 3px;
  color: #717687;
  font-size: 10px;
}

.analytics-recent-status {
  flex-shrink: 0;
  text-align: right;
}

.analytics-recent-status span {
  display: inline-block;
  padding: 5px 8px;
  border-radius: 999px;
  color: #bbaeff;
  background: rgba(126, 96, 255, 0.1);
  font-size: 9px;
  font-weight: 700;
}

.analytics-recent-status small {
  display: block;
  margin-top: 4px;
  color: #656a7a;
  font-size: 9px;
}

@media (max-width: 1100px) {
  .analytics-summary-grid {
    grid-template-columns: 1fr 1fr;
  }

  .analytics-main-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 700px) {
  .analytics-summary-grid {
    grid-template-columns: 1fr;
  }

  .analytics-page-header {
    align-items: flex-start;
    flex-direction: column;
  }

  .analytics-health {
    grid-template-columns: 1fr;
  }

  .analytics-health-ring {
    margin: 0 auto;
  }

  .analytics-funnel-row {
    grid-template-columns: 85px minmax(0, 1fr) 35px;
  }
}

/* =========================================================
   DASHBOARD INTERACTION
   ========================================================= */

.stat-card {
  font: inherit;
  text-align: left;
  color: inherit;
}

.stat-card-clickable {
  cursor: pointer;
  transition:
    transform 0.18s ease,
    border-color 0.18s ease,
    background 0.18s ease;
}

.stat-card-clickable:hover {
  transform: translateY(-2px);
  border-color: rgba(139, 108, 255, 0.3);
  background: rgba(17, 19, 29, 0.95);
}

.stat-card-clickable:focus-visible,
.application-row-clickable:focus-visible,
.upcoming-item-clickable:focus-visible {
  outline: 2px solid #8064ff;
  outline-offset: 2px;
}

.application-row-clickable {
  cursor: pointer;
  transition:
    background 0.18s ease,
    border-color 0.18s ease;
}

.application-row-clickable:hover {
  background: rgba(126, 96, 255, 0.045);
}

.more-button {
  position: relative;
  z-index: 2;
}

.upcoming-item-clickable {
  width: 100%;
  border: 0;
  color: inherit;
  font: inherit;
  text-align: left;
  cursor: pointer;
  transition:
    background 0.18s ease,
    transform 0.18s ease;
}

.upcoming-item-clickable:hover {
  background: rgba(126, 96, 255, 0.045);
}

.upcoming-item-clickable:hover
  .upcoming-arrow {
  color: #a993ff;
  transform: translateX(2px);
}

.upcoming-arrow {
  transition:
    color 0.18s ease,
    transform 0.18s ease;
}

.dashboard-icon-button {
  display: grid;
  place-items: center;
  width: 32px;
  height: 32px;
  padding: 0;
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 8px;
  background: transparent;
  color: #777c8d;
  cursor: pointer;
  transition:
    color 0.18s ease,
    border-color 0.18s ease,
    background 0.18s ease;
}

.dashboard-icon-button:hover {
  color: #a993ff;
  border-color: rgba(139, 108, 255, 0.28);
  background: rgba(126, 96, 255, 0.08);
}

.upgrade-card {
  width: 100%;
  border: 1px solid rgba(126, 96, 255, 0.18);
  color: inherit;
  font: inherit;
  text-align: left;
  cursor: pointer;
}

.upgrade-card:hover {
  border-color: rgba(126, 96, 255, 0.38);
  background: rgba(126, 96, 255, 0.08);
}

.profile {
  width: 100%;
  border: 0;
  color: inherit;
  font: inherit;
  text-align: left;
  cursor: pointer;
}

.profile:hover {
  background: rgba(255, 255, 255, 0.035);
}

/* =========================================================
   SETTINGS PAGE
   ========================================================= */

.settings-page {
  width: 100%;
  padding-bottom: 40px;
  animation: pageFadeIn 0.25s ease;
}

.settings-page-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 24px;
}

.settings-page-header h2 {
  margin: 6px 0 8px;
  color: #f4f5f8;
  font-size: 32px;
  line-height: 1.1;
  letter-spacing: -0.8px;
}

.settings-header-icon {
  display: grid;
  place-items: center;
  width: 48px;
  height: 48px;
  border: 1px solid rgba(139, 108, 255, 0.2);
  border-radius: 14px;
  background: rgba(126, 96, 255, 0.1);
  color: #a993ff;
}

.settings-alert {
  display: flex;
  align-items: center;
  gap: 9px;
  margin-bottom: 18px;
  padding: 12px 15px;
  border-radius: 12px;
  font-size: 12px;
}

.settings-alert-error {
  border: 1px solid rgba(248, 113, 113, 0.28);
  background: rgba(127, 29, 29, 0.16);
  color: #fecaca;
}

.settings-alert-success {
  border: 1px solid rgba(74, 222, 128, 0.22);
  background: rgba(34, 197, 94, 0.08);
  color: #86efac;
}

.settings-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.settings-card {
  min-width: 0;
  padding: 21px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 16px;
  background: rgba(12, 14, 21, 0.88);
}

.settings-card-wide {
  grid-column: 1 / -1;
}

.settings-card-header {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  margin-bottom: 20px;
}

.settings-card-header h3 {
  margin: 2px 0 5px;
  color: #f1f2f6;
  font-size: 16px;
}

.settings-card-header p {
  margin: 0;
  color: #737b8e;
  font-size: 11px;
  line-height: 1.5;
}

.settings-section-icon {
  display: grid;
  place-items: center;
  width: 38px;
  height: 38px;
  flex: 0 0 38px;
  border-radius: 11px;
}

.settings-section-icon.purple {
  background: rgba(139, 92, 246, 0.12);
  color: #a98bff;
}

.settings-section-icon.blue {
  background: rgba(59, 130, 246, 0.12);
  color: #75a7ff;
}

.settings-section-icon.orange {
  background: rgba(245, 158, 11, 0.12);
  color: #ffb45e;
}

.settings-section-icon.slate {
  background: rgba(148, 163, 184, 0.1);
  color: #aab5c7;
}

.settings-form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.settings-field {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.settings-field-full {
  grid-column: 1 / -1;
}

.settings-field label {
  color: #aab2c1;
  font-size: 11px;
  font-weight: 700;
}

.settings-field input,
.settings-field textarea,
.settings-select-row select {
  width: 100%;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 10px;
  outline: none;
  background: rgba(255, 255, 255, 0.035);
  color: #e8eaf0;
  font: inherit;
  font-size: 12px;
  transition:
    border-color 0.18s ease,
    background 0.18s ease;
}

.settings-field input,
.settings-field textarea {
  padding: 11px 12px;
}

.settings-field textarea {
  min-height: 100px;
  resize: vertical;
  line-height: 1.55;
}

.settings-field input::placeholder,
.settings-field textarea::placeholder {
  color: #555d6e;
}

.settings-field input:focus,
.settings-field textarea:focus,
.settings-select-row select:focus {
  border-color: rgba(139, 108, 255, 0.55);
  background: rgba(126, 96, 255, 0.045);
}

.settings-field small {
  color: #626b7e;
  font-size: 10px;
}

.settings-option-list {
  display: flex;
  flex-direction: column;
}

.settings-select-row,
.settings-toggle-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 14px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.055);
}

.settings-select-row:first-child,
.settings-toggle-row:first-child {
  padding-top: 0;
}

.settings-select-row:last-child,
.settings-toggle-row:last-child {
  border-bottom: 0;
  padding-bottom: 0;
}

.settings-select-row strong,
.settings-toggle-row strong {
  display: block;
  margin-bottom: 4px;
  color: #e4e6ec;
  font-size: 12px;
}

.settings-select-row span,
.settings-toggle-row span {
  display: block;
  max-width: 310px;
  color: #697286;
  font-size: 10px;
  line-height: 1.45;
}

.settings-select-row select {
  width: 130px;
  flex-shrink: 0;
  padding: 9px 10px;
}

.settings-select-row select option {
  background: #141720;
  color: #e8eaf0;
}

.settings-toggle-row.disabled {
  opacity: 0.42;
}

.settings-switch {
  position: relative;
  width: 42px;
  height: 24px;
  flex: 0 0 42px;
  padding: 0;
  border: 0;
  border-radius: 999px;
  background: #2a2f3a;
  cursor: pointer;
  transition: background 0.18s ease;
}

.settings-switch span {
  position: absolute;
  top: 4px;
  left: 4px;
  display: block;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #8992a4;
  transition:
    transform 0.18s ease,
    background 0.18s ease;
}

.settings-switch.active {
  background: rgba(126, 96, 255, 0.65);
}

.settings-switch.active span {
  transform: translateX(18px);
  background: #ffffff;
}

.settings-switch:disabled {
  cursor: not-allowed;
}

.settings-ai-note {
  display: flex;
  align-items: flex-start;
  gap: 9px;
  margin-top: 18px;
  padding: 11px 12px;
  border: 1px solid rgba(139, 92, 246, 0.16);
  border-radius: 10px;
  background: rgba(139, 92, 246, 0.055);
  color: #8f96aa;
  font-size: 10px;
  line-height: 1.5;
}

.settings-ai-note svg {
  flex-shrink: 0;
  color: #9c83ff;
}

.settings-theme-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.settings-theme-option {
  padding: 10px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.025);
  color: inherit;
  text-align: left;
  cursor: pointer;
  transition:
    border-color 0.18s ease,
    background 0.18s ease;
}

.settings-theme-option:hover:not(.disabled) {
  border-color: rgba(139, 108, 255, 0.35);
  background: rgba(126, 96, 255, 0.045);
}

.settings-theme-option.active {
  border-color: rgba(139, 108, 255, 0.55);
  background: rgba(126, 96, 255, 0.075);
}

.settings-theme-option.disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

.settings-theme-option strong {
  display: block;
  margin-top: 10px;
  color: #e4e6ed;
  font-size: 12px;
}

.settings-theme-option small {
  display: block;
  margin-top: 3px;
  color: #687185;
  font-size: 9px;
}

.theme-preview {
  display: flex;
  align-items: flex-end;
  gap: 5px;
  height: 58px;
  padding: 9px;
  overflow: hidden;
  border-radius: 8px;
}

.theme-preview.dark {
  background: #0b0d13;
}

.theme-preview.light {
  background: #e7eaf0;
}

.theme-preview span {
  display: block;
  height: 22px;
  flex: 1;
  border-radius: 4px;
  background: rgba(255, 255, 255, 0.12);
}

.theme-preview span:nth-child(2) {
  height: 32px;
  background: rgba(126, 96, 255, 0.45);
}

.theme-preview span:nth-child(3) {
  height: 17px;
  background: rgba(255, 255, 255, 0.08);
}

.settings-save-bar {
  position: sticky;
  bottom: 16px;
  z-index: 5;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
  margin-top: 18px;
  padding: 14px 16px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 14px;
  background: rgba(14, 16, 24, 0.94);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.28);
  backdrop-filter: blur(14px);
}

.settings-save-bar strong {
  display: block;
  margin-bottom: 3px;
  color: #e8eaf0;
  font-size: 12px;
}

.settings-save-bar span {
  color: #687286;
  font-size: 10px;
}

.settings-save-actions {
  display: flex;
  align-items: center;
  gap: 9px;
}

.settings-reset-button,
.settings-save-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  min-height: 38px;
  padding: 0 14px;
  border-radius: 9px;
  font: inherit;
  font-size: 11px;
  font-weight: 700;
  cursor: pointer;
}

.settings-reset-button {
  border: 1px solid rgba(255, 255, 255, 0.08);
  background: transparent;
  color: #9aa3b5;
}

.settings-reset-button:hover {
  background: rgba(255, 255, 255, 0.035);
  color: #d8dce5;
}

.settings-save-button {
  border: 1px solid rgba(139, 108, 255, 0.35);
  background: linear-gradient(
    135deg,
    #7356e8,
    #8c70ff
  );
  color: #ffffff;
  box-shadow: 0 8px 22px rgba(126, 96, 255, 0.18);
}

.settings-save-button:hover {
  filter: brightness(1.08);
}

.settings-reset-button:disabled,
.settings-save-button:disabled {
  opacity: 0.55;
  cursor: not-allowed;
}

.settings-loading {
  display: flex;
  min-height: 360px;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 12px;
  color: #778198;
  font-size: 12px;
}

.settings-loading-spinner {
  width: 25px;
  height: 25px;
  border: 2px solid rgba(139, 108, 255, 0.15);
  border-top-color: #8b6cff;
  border-radius: 50%;
  animation: settingsSpin 0.8s linear infinite;
}

@keyframes settingsSpin {
  to {
    transform: rotate(360deg);
  }
}

@media (max-width: 850px) {
  .settings-grid {
    grid-template-columns: 1fr;
  }

  .settings-card-wide {
    grid-column: auto;
  }

  .settings-form-grid {
    grid-template-columns: 1fr;
  }

  .settings-field-full {
    grid-column: auto;
  }
}

@media (max-width: 600px) {
  .settings-page-header {
    align-items: flex-start;
  }

  .settings-header-icon {
    display: none;
  }

  .settings-select-row,
  .settings-toggle-row {
    align-items: flex-start;
  }

  .settings-select-row select {
    width: 110px;
  }

  .settings-save-bar {
    align-items: stretch;
    flex-direction: column;
  }

  .settings-save-actions {
    width: 100%;
  }

  .settings-reset-button,
  .settings-save-button {
    flex: 1;
  }
}

/* =========================================================
   PROFILE MENU
========================================================= */

.profile-wrapper {
  position: relative;
  width: 100%;
}

.profile {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 10px;

  border: 0;
  cursor: pointer;
  text-align: left;
  background: transparent;
  font: inherit;

  padding: 8px;
  border-radius: 10px;

  color: var(--text);
}

.profile:hover {
  background: rgba(255, 255, 255, 0.045);
}

.profile:focus-visible {
  outline: 2px solid rgba(124, 92, 255, 0.55);
  outline-offset: 2px;
}

.profile-avatar {
  width: 32px;
  height: 32px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 9px;

  background: linear-gradient(
    135deg,
    #7c5cff,
    #a875ff
  );

  color: white;

  font-size: 12px;
  font-weight: 800;
}

.profile-info {
  min-width: 0;
  flex: 1;
}

.profile-info strong {
  display: block;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--text);

  font-size: 12px;
  font-weight: 700;
}

.profile-info span {
  display: block;

  margin-top: 2px;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--text-muted);

  font-size: 10px;
}

.profile-chevron-open {
  transform: rotate(180deg);
  transition: transform 0.18s ease;
}

/* =========================
   PROFILE DROPDOWN
========================= */

.profile-menu {
  position: absolute;

  left: 0;
  bottom: calc(100% + 10px);

  width: 260px;

  padding: 12px;

  background: #11141c;

  border: 1px solid rgba(255, 255, 255, 0.09);
  border-radius: 14px;

  box-shadow:
    0 20px 50px rgba(0, 0, 0, 0.45),
    0 0 0 1px rgba(124, 92, 255, 0.04);

  z-index: 100;
}

.profile-menu-header {
  display: flex;
  align-items: center;
  gap: 10px;

  padding: 5px 4px 10px;
}

.profile-menu-avatar {
  width: 40px;
  height: 40px;

  flex-shrink: 0;

  display: grid;
  place-items: center;

  border-radius: 11px;

  background: linear-gradient(
    135deg,
    #7c5cff,
    #a875ff
  );

  color: white;

  font-size: 14px;
  font-weight: 800;
}

.profile-menu-header strong {
  display: block;

  color: var(--text);

  font-size: 13px;
}

.profile-menu-header span {
  display: block;

  margin-top: 3px;

  color: var(--text-muted);

  font-size: 10px;
}

.profile-menu-divider {
  height: 1px;

  margin: 8px 0;

  background: rgba(255, 255, 255, 0.07);
}

/* =========================
   PROFILE DETAILS
========================= */

.profile-details {
  display: flex;
  flex-direction: column;
  gap: 8px;

  padding: 4px;
}

.profile-detail {
  display: flex;
  align-items: center;
  justify-content: space-between;

  gap: 15px;
}

.profile-detail span {
  color: var(--text-muted);
  font-size: 10px;
}

.profile-detail strong {
  max-width: 150px;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--text-soft);

  font-size: 10px;
  font-weight: 600;

  text-align: right;
}

/* =========================
   PROFILE MENU BUTTONS
========================= */

.profile-menu-item {
  width: 100%;

  display: flex;
  align-items: center;
  gap: 10px;

  padding: 9px 8px;

  border: 0;
  border-radius: 8px;

  background: transparent;

  color: var(--text-soft);

  font: inherit;
  font-size: 11px;

  cursor: pointer;

  text-align: left;
}

.profile-menu-item:hover {
  background: rgba(255, 255, 255, 0.05);
  color: var(--text);
}

.profile-menu-item svg {
  flex-shrink: 0;
}

.profile-logout {
  color: #ff7b86;
}

.profile-logout:hover {
  background: rgba(255, 109, 122, 0.08);
  color: #ff8b95;
}

/* ================================
   REAL NOTIFICATIONS
================================ */

.notification-item-button {
  width: 100%;
  border: 0;
  padding: 0;
  margin: 0;
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
  display: block;
}

.notification-item-button:hover .notification-item {
  background: #191c26;
}

.notification-item {
  transition:
    background 0.18s ease,
    transform 0.18s ease;
}

.notification-item-button:focus-visible {
  outline: 2px solid #7c5cff;
  outline-offset: -2px;
  border-radius: 10px;
}

.notification-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 28px 22px;
  gap: 8px;
}

.notification-empty-icon {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #181b25;
  color: #8b7cff;
  margin-bottom: 4px;
}

.notification-empty strong {
  color: #f3f4f6;
  font-size: 13px;
}

.notification-empty span {
  color: #777d8d;
  font-size: 11px;
  line-height: 1.55;
  max-width: 220px;
}

/* =========================================================
   RESPONSIVE LAYOUT — INTERNFLOW
========================================================= */

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body,
#root {
  width: 100%;
  min-width: 0;
  margin: 0;
}

body {
  overflow-x: hidden;
}

.app-shell {
  width: 100%;
  min-height: 100vh;
  min-width: 0;
}

.sidebar {
  flex-shrink: 0;
}

.main-content {
  min-width: 0;
  width: 100%;
  overflow-x: hidden;
}

.page-content {
  width: 100%;
  min-width: 0;
  max-width: 100%;
}

.topbar {
  min-width: 0;
}

.topbar-actions {
  min-width: 0;
}

.search-box {
  min-width: 0;
  max-width: 360px;
}

.search-box input {
  min-width: 0;
  width: 100%;
}

/* ---------------------------------------------------------
   LARGE LAPTOP / DESKTOP
--------------------------------------------------------- */

@media (max-width: 1400px) {
  .sidebar {
    width: 220px;
  }

  .main-content {
    width: calc(100% - 220px);
  }

  .page-content {
    padding-left: 28px;
    padding-right: 28px;
  }

  .dashboard-grid,
  .stats-grid {
    gap: 14px;
  }
}

/* ---------------------------------------------------------
   LAPTOP
--------------------------------------------------------- */

@media (max-width: 1100px) {
  .sidebar {
    width: 200px;
  }

  .main-content {
    width: calc(100% - 200px);
  }

  .page-content {
    padding-left: 20px;
    padding-right: 20px;
  }

  .search-box {
    max-width: 250px;
  }

  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  .stats-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

/* ---------------------------------------------------------
   TABLET
--------------------------------------------------------- */

@media (max-width: 820px) {
  .app-shell {
    display: block;
  }

  .sidebar {
    position: fixed;
    left: 0;
    top: 0;
    bottom: 0;
    width: 72px;
    z-index: 100;
    overflow: hidden;
  }

  .sidebar .brand {
    justify-content: center;
    padding-left: 10px;
    padding-right: 10px;
  }

  .sidebar .brand > div:last-child,
  .sidebar .section-label,
  .sidebar-item span,
  .sidebar-bottom .upgrade-card > div:last-child,
  .sidebar-bottom .profile-info,
  .sidebar-bottom .profile > svg {
    display: none;
  }

  .sidebar-item {
    justify-content: center;
    padding-left: 0;
    padding-right: 0;
  }

  .sidebar-badge {
    position: absolute;
    top: 5px;
    right: 8px;
  }

  .sidebar-bottom {
    padding-left: 8px;
    padding-right: 8px;
  }

  .upgrade-card,
  .profile {
    justify-content: center;
  }

  .main-content {
    width: calc(100% - 72px);
    margin-left: 72px;
  }

  .topbar {
    padding-left: 18px;
    padding-right: 18px;
  }

  .breadcrumb {
    display: none;
  }

  .topbar-actions {
    width: 100%;
    justify-content: flex-end;
  }

  .search-box {
    flex: 1;
    max-width: none;
  }

  .page-content {
    padding: 20px;
  }

  .stats-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  .settings-form-grid {
    grid-template-columns: 1fr;
  }

  .settings-theme-grid {
    grid-template-columns: 1fr 1fr;
  }
}

/* ---------------------------------------------------------
   MOBILE
--------------------------------------------------------- */

@media (max-width: 600px) {
  .sidebar {
    width: 0;
    display: none;
  }

  .main-content {
    width: 100%;
    margin-left: 0;
  }

  .topbar {
    height: auto;
    min-height: 64px;
    padding: 10px 12px;
  }

  .topbar-actions {
    gap: 8px;
  }

  .search-box {
    min-width: 0;
    height: 40px;
  }

  .search-shortcut {
    display: none;
  }

  .icon-button {
    flex: 0 0 40px;
  }

  .topbar-actions > .submit-button,
  .topbar-actions > .add-button,
  .topbar-actions .primary-button {
    padding-left: 10px;
    padding-right: 10px;
  }

  .page-content {
    padding: 14px;
  }

  .stats-grid {
    grid-template-columns: 1fr;
  }

  .dashboard-grid,
  .applications-layout,
  .calendar-layout,
  .analytics-grid {
    grid-template-columns: 1fr !important;
  }

  .settings-form-grid {
    grid-template-columns: 1fr;
  }

  .settings-theme-grid {
    grid-template-columns: 1fr;
  }

  .settings-save-bar {
    position: sticky;
    bottom: 8px;
    flex-direction: column;
    align-items: stretch;
  }

  .settings-save-actions {
    width: 100%;
  }

  .settings-save-actions button {
    flex: 1;
  }

  .notification-panel {
    position: fixed;
    top: 68px;
    left: 12px;
    right: 12px;
    width: auto;
    max-width: none;
  }

  .modal-overlay {
    padding: 12px;
  }

  .modal,
  .application-modal,
  .ai-modal {
    width: 100%;
    max-width: 100%;
    max-height: calc(100vh - 24px);
    overflow-y: auto;
  }

  .application-card,
  .application-row {
    min-width: 0;
  }

  .calendar-page {
    overflow-x: auto;
  }

  .calendar-grid {
    min-width: 650px;
  }
}

/* ---------------------------------------------------------
   VERY SMALL PHONES
--------------------------------------------------------- */

@media (max-width: 420px) {
  .page-content {
    padding: 10px;
  }

  .topbar {
    padding-left: 8px;
    padding-right: 8px;
  }

  .search-box {
    max-width: none;
  }

  .search-box svg {
    display: none;
  }

  .notification-panel {
    left: 8px;
    right: 8px;
  }

  .settings-card {
    padding: 14px;
  }

  .settings-card-header {
    gap: 10px;
  }
}

/* =========================================================
   LIGHT THEME
========================================================= */

:root,
html[data-theme="dark"],
body[data-theme="dark"] {
  color-scheme: dark;
}

html[data-theme="light"],
body[data-theme="light"] {
  color-scheme: light;
}

/* Main application */

body[data-theme="light"] {
  background: #f5f7fb;
  color: #202431;
}

body[data-theme="light"] .app-shell {
  background: #f5f7fb;
  color: #202431;
}

/* Sidebar */

body[data-theme="light"] .sidebar {
  background: #ffffff;
  border-right-color: #e5e7eb;
}

body[data-theme="light"] .brand h1,
body[data-theme="light"] .brand span,
body[data-theme="light"] .section-label {
  color: #202431;
}

body[data-theme="light"] .sidebar-item {
  color: #667085;
}

body[data-theme="light"] .sidebar-item:hover {
  background: #f3f4f8;
  color: #202431;
}

body[data-theme="light"] .sidebar-item.active {
  background: #f0edff;
  color: #5d45d8;
}

body[data-theme="light"] .profile {
  background: transparent;
  color: #202431;
}

body[data-theme="light"] .profile-info span {
  color: #7b8494;
}

body[data-theme="light"] .upgrade-card {
  background: #f7f5ff;
  border-color: #e5ddff;
}

/* Main */

body[data-theme="light"] .main-content {
  background: #f5f7fb;
}

body[data-theme="light"] .topbar {
  background: rgba(255, 255, 255, 0.92);
  border-bottom-color: #e5e7eb;
}

body[data-theme="light"] .breadcrumb {
  color: #7b8494;
}

body[data-theme="light"] .breadcrumb strong {
  color: #202431;
}

/* Search */

body[data-theme="light"] .search-box {
  background: #ffffff;
  border-color: #dfe3eb;
}

body[data-theme="light"] .search-box input {
  color: #202431;
}

body[data-theme="light"] .search-box input::placeholder {
  color: #9aa3b2;
}

/* Cards */

body[data-theme="light"] .stat-card,
body[data-theme="light"] .dashboard-card,
body[data-theme="light"] .settings-card,
body[data-theme="light"] .calendar-card,
body[data-theme="light"] .analytics-card {
  background: #ffffff;
  border-color: #e4e7ee;
  color: #202431;
  box-shadow: 0 4px 18px rgba(15, 23, 42, 0.04);
}

/* Headings */

body[data-theme="light"] h1,
body[data-theme="light"] h2,
body[data-theme="light"] h3,
body[data-theme="light"] h4,
body[data-theme="light"] strong {
  color: #202431;
}

/* Secondary text */


/* Application rows */

body[data-theme="light"] .application-row,
body[data-theme="light"] .application-card {
  background: #ffffff;
  border-color: #e7e9ef;
}

body[data-theme="light"] .application-row:hover,
body[data-theme="light"] .application-card:hover {
  background: #fafbfe;
}

/* Upcoming events */

body[data-theme="light"] .upcoming-event {
  background: #ffffff !important;
  color: #202431 !important;
  border-bottom-color: #e5e7eb;
}

body[data-theme="light"] .upcoming-event:hover {
  background: #f7f8fb !important;
}

body[data-theme="light"] .upcoming-event-info strong {
  color: #202431;
}

body[data-theme="light"] .upcoming-event-info span,
body[data-theme="light"] .upcoming-event-info small {
  color: #7b8494;
}

/* Notification */

body[data-theme="light"] .notification-panel {
  background: #ffffff;
  border-color: #e1e4eb;
  box-shadow: 0 18px 50px rgba(15, 23, 42, 0.14);
}

body[data-theme="light"] .notification-header {
  border-bottom-color: #e5e7eb;
}

body[data-theme="light"] .notification-header strong {
  color: #202431;
}

/* Settings */

body[data-theme="light"] .settings-card {
  background: #ffffff;
  border-color: #e4e7ee;
}

body[data-theme="light"] .settings-card-header h2,
body[data-theme="light"] .settings-card-header h3 {
  color: #202431;
}

body[data-theme="light"] .settings-card-header p,
body[data-theme="light"] .settings-field small,
body[data-theme="light"] .settings-toggle-row span,
body[data-theme="light"] .settings-select-row span {
  color: #7b8494;
}

body[data-theme="light"] .settings-field input,
body[data-theme="light"] .settings-field textarea,
body[data-theme="light"] .settings-select-row select {
  background: #f8fafc;
  border-color: #dfe3eb;
  color: #202431;
}

body[data-theme="light"] .settings-field input::placeholder,
body[data-theme="light"] .settings-field textarea::placeholder {
  color: #a0a8b6;
}

body[data-theme="light"] .settings-select-row,
body[data-theme="light"] .settings-toggle-row {
  border-bottom-color: #edf0f4;
}

body[data-theme="light"] .settings-theme-option {
  background: #f8fafc;
  border-color: #e1e5ec;
}

body[data-theme="light"] .settings-theme-option.active {
  background: #f2efff;
  border-color: #8b6cff;
}

body[data-theme="light"] .settings-theme-option strong {
  color: #202431;
}

body[data-theme="light"] .settings-save-bar {
  background: rgba(255, 255, 255, 0.94);
  border-color: #e1e4eb;
  box-shadow: 0 12px 40px rgba(15, 23, 42, 0.1);
}

body[data-theme="light"] .settings-save-bar strong {
  color: #202431;
}

/* Calendar */

body[data-theme="light"] .calendar-day {
  background: #ffffff;
  border-color: #e7e9ef;
}

body[data-theme="light"] .calendar-day-number {
  color: #667085;
}

body[data-theme="light"] .calendar-day.other-month {
  background: #fafbfc;
}

body[data-theme="light"] .calendar-sidebar-card {
  background: #ffffff;
  border-color: #e4e7ee;
}

/* Modal */

body[data-theme="light"] .modal,
body[data-theme="light"] .application-modal,
body[data-theme="light"] .ai-modal {
  background: #ffffff;
  color: #202431;
  border-color: #e1e4eb;
}

body[data-theme="light"] .modal-header {
  border-bottom-color: #e5e7eb;
}

body[data-theme="light"] .modal-footer {
  border-top-color: #e5e7eb;
}

/* Theme preview */

body[data-theme="light"] .theme-preview.light {
  background: #eef0f5;
}

body[data-theme="light"] .theme-preview.light span {
  background: rgba(50, 55, 70, 0.12);
}

body[data-theme="light"] .theme-preview.light span:nth-child(2) {
  background: rgba(126, 96, 255, 0.45);
}

.notification-content {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.notification-content strong {
  font-size: 13px;
}

.notification-content span {
  font-size: 11px;
  opacity: 0.7;
}

.notification-date {
  font-size: 10px;
  opacity: 0.55;
  margin-top: 2px;
}